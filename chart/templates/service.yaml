apiVersion: v1
kind: Service
metadata:
  name: {{ include "fipsagents-platform.fullname" . }}
  labels:
    {{- include "fipsagents-platform.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fipsagents-platform.selectorLabels" . | nindent 4 }}
